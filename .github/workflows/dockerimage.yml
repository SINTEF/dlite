name: Docker Image CI

on: [push]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Get docker version
      run: docker --version
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag dlite-ci$(date +%s)
