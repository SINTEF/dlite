name: Weekly build of docker images

on:
  schedule:
    - cron: "42 5 * * 1"
  workflow_dispatch:
    inputs:
      use_cache:
        description: Use GH Actions cache
        type: boolean
        required: false
        default: true

env:
  CONTAINER_REGISTRY: ghcr.io

jobs:

  parse_matrix:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.build.outputs.matrix }}
    steps:
      - uses: actions/checkout@v4

      - name: Convert images table to JSON
        id: build
        run: |
          python .github/docker/table2json.py .github/docker/images.txt >> $GITHUB_OUTPUT

  build_linux_dockers:
    name: Build containers
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
        # 64-bit manylinux
        - system: "manylinux"
          type: "_2_28"
          arch: "x86_64"
          py_minors: "8 9 10 11 12 13 14"
        #- system: "manylinux"
        #  type: "_2_28"
        #  arch: "x86_64"
        #  py_minors: "8 9 10 11 12"
        #- system: "manylinux"
        #  type: "2014"
        #  arch: "x86_64"
        #  py_minors: "8 9 10 11 12"
        #- system: "manylinux"
        #  type: "2010"
        #  arch: "x86_64"
        #  py_minors: "8 9"

        # 64-bit musllinux
        - system: "musllinux"
          type: "_1_2"
          arch: "x86_64"
          py_minors: "8 9 10 11 12 13 14"
        #- system: "musllinux"
        #  type: "_1_1"
        #  arch: "x86_64"
        #  py_minors: "8 9 10 11"
        #- system: "musllinux"
        #  type: "_1_2"
        #  arch: "x86_64"
        #  py_minors: "8 9 10 11"

        # 32-bit manylinux
        - system: "manylinux"
          type: "2014"
          arch: "i686"
          py_minors: "10 11 12 13 14"
        #- system: "manylinux"
        #  type: "2010"
        #  arch: "i686"
        #  py_minors: "8 9"

        # 32-bit musllinux
        # Seems that pyyaml depends on distutils, which is only available on Python 3.11 and below
        - system: "musllinux"
          type: "_1_2"
          arch: "i686"
          py_minors: "9 10 11"
        #- system: "musllinux"
        #  type: "_1_1"
        #  arch: "i686"
        #  py_minors: "8 9 10 11"

    steps:
      - name: Define environment variables
        run: |
          # System string
          echo "SYSTEM=${{ matrix.system }}${{ matrix.type }}_${{ matrix.arch }}"
          # Relative path to Dockerfile to build
          echo "DOCKERFILE=docker_build/Dockerfile-$SYSTEM" >> "$GITHUB_ENV"
          # Filename to store SHA hash of the Dockerfile
          echo "SHA_FILENAME=$SYSTEM:latest.sha.txt" >> "$GITHUB_ENV"
          # The base image to use when building the docker container
          echo "IMAGE=quay.io/pypa/$SYSTEM:latest" >> "$GITHUB_ENV"
          # Cache directory for checking whether to (re)build docker container
          echo "CACHE_DIR=.buildx-cache-$SYSTEM" >> "$GITHUB_ENV"

      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Docker buildx
        uses: docker/setup-buildx-action@v3

      - name: Setup cache
        if: github.event_name != 'workflow_dispatch' || fromJSON(github.event.inputs.use_cache)
        uses: actions/cache@v5
        with:
          path: .buildx-cache-$SYSTEM
          #key: buildx-cache-${{ matrix.system }}${{ matrix.type }}-${{ matrix.arch }}-${{ github.sha }}
          #restore-keys: buildx-cache-${{ matrix.system }}${{ matrix.type }}-${{ matrix.arch }}-
          key: buildx-cache-${SYSTEM}-${{ github.sha }}
          restore-keys: buildx-cache-${SYSTEM}-

      - name: Create docker file
        run: |
          mkdir -p docker_build
          bash .github/docker/gen_dockerfile.sh ${{ matrix.system }} ${{ matrix.type }} ${{ matrix.arch }} "${{ matrix.py_minors }}" \
            > ${DOCKERFILE}

      - name: Create file with SHA hashes
        run: |
          sha256sum ${DOCKERFILE} > ${SHA_FILENAME}

      - name: Check whether to build new image
        run: |
          docker pull ${IMAGE}
          docker images --no-trunc --quiet ${IMAGE} >> ${SHA_FILENAME}
          if [ -f "${CACHE_DIR}/${SHA_FILENAME}" ] && [ "$( cat ${SHA_FILENAME} )" == "$( cat ${CACHE_DIR}/${SHA_FILENAME} )" ]; then
            echo "BUILD_NEW_IMAGE=false" >> $GITHUB_ENV
          else
            echo "BUILD_NEW_IMAGE=true" >> $GITHUB_ENV
          fi

      - name: Login to GitHub Container Registry
        if: env.BUILD_NEW_IMAGE == 'true'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.CONTAINER_REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        if: env.BUILD_NEW_IMAGE == 'true'
        uses: docker/build-push-action@v6
        with:
          context: .
          file: docker_build/Dockerfile-$SYSTEM
          tags: ${{ env.CONTAINER_REGISTRY }}/sintef/dlite-python-$SYSTEM:latest
          push: true
          build-args: |
            PY_MINORS=${{ matrix.py_minors }}
          cache-from: type=local,src=.buildx-cache-$SYSTEM
          cache-to: type=local,dest=.buildx-cache-$SYSTEM-new,mode=max

      # Temporary fix from https://github.com/docker/build-push-action/blob/master/docs/advanced/cache.md
      # https://github.com/docker/build-push-action/issues/252
      # https://github.com/moby/buildkit/issues/1896
      - name: Move cache
        if: env.BUILD_NEW_IMAGE == 'true'
        run: |
          rm -rf .buildx-cache-$SYSTEM
          mv .buildx-cache-$SYSTEM-new .buildx-cache-$SYSTEM

      - name: Add sha.txt to cache
        if: env.BUILD_NEW_IMAGE == 'true'
        run: mv $SYSTEM:latest.sha.txt .buildx-cache-$SYSTEM
